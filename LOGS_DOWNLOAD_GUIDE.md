# 📥 Руководство по скачиванию логов

## Проблема с FileZilla

Если вы скачали файл лога через FileZilla и не можете его открыть в редакторе, это обычно связано с неправильным режимом передачи файла.

### Причины проблемы:

1. **Режим передачи** - файл был скачан в текстовом режиме (ASCII) вместо бинарного (Binary)
2. **Кодировка** - файлы логов в UTF-8, но при передаче в текстовом режиме могут быть повреждены
3. **Окончания строк** - могут быть неправильно преобразованы между Unix (LF) и Windows (CRLF)

## ✅ Решение 1: Использование API (Рекомендуется)

Используйте новый API эндпоинт для скачивания логов - он гарантирует правильную передачу файла.

### Получить список логов:
```
GET /logs/list
```

**Требования:**
- Авторизация: требуется (токен администратора)
- Cookie: `users_access_token`

**Ответ:**
```json
{
  "logs": [
    {
      "filename": "app_2026-01-02.log",
      "size": 1234567,
      "modified": 1704240000.0,
      "type": "log"
    },
    {
      "filename": "app_2026-01-01.log.zip",
      "size": 456789,
      "modified": 1704153600.0,
      "type": "archive"
    }
  ]
}
```

### Скачать файл лога:
```
GET /logs/download/{filename}
```

**Пример:**
```
GET /logs/download/app_2026-01-02.log
```

**Параметры:**
- `filename` - имя файла лога (например, `app_2026-01-02.log`)

**Требования:**
- Авторизация: требуется (токен администратора)
- Cookie: `users_access_token`

**Ответ:** Файл будет скачан с правильной кодировкой UTF-8

### Скачать большой файл (streaming):
```
GET /logs/download-stream/{filename}
```

Используйте этот эндпоинт для больших файлов - он передает файл по частям, не загружая его полностью в память.

## ✅ Решение 2: Правильная настройка FileZilla

Если вы все же хотите использовать FileZilla, настройте его правильно:

### Шаг 1: Установите бинарный режим передачи

1. Откройте FileZilla
2. Перейдите в меню **Edit** → **Settings** (или **Настройки**)
3. Выберите **Transfers** → **File Types**
4. Установите:
   - **Default transfer type**: **Binary**
   - Или добавьте правило: **File extension**: `.log`, **Transfer type**: **Binary**

### Шаг 2: Установите бинарный режим для текущей сессии

1. В главном окне FileZilla найдите переключатель режима передачи
2. Убедитесь, что выбран режим **Binary** (не ASCII)
3. Или используйте горячую клавишу: **Alt+B**

### Шаг 3: Скачайте файл заново

1. Удалите поврежденный файл
2. Скачайте файл снова в бинарном режиме
3. Файл должен открываться корректно

## ✅ Решение 3: Исправление уже скачанного файла

Если файл уже скачан неправильно, можно попробовать исправить его:

### В PowerShell (Windows):
```powershell
# Перекодировать файл в UTF-8
Get-Content "app_2026-01-02.log" -Encoding Default | Set-Content "app_2026-01-02_fixed.log" -Encoding UTF8
```

### В Linux/Mac:
```bash
# Исправить окончания строк и кодировку
iconv -f ISO-8859-1 -t UTF-8 app_2026-01-02.log > app_2026-01-02_fixed.log
```

## 🔒 Безопасность

- Доступ к логам имеют только администраторы
- Проверка безопасности имени файла (защита от path traversal)
- Файлы могут быть скачаны только из директории `logs/`

## 📝 Примеры использования API

### cURL:
```bash
# Получить список логов
curl -X GET "http://your-server:8000/logs/list" \
  -H "Cookie: users_access_token=YOUR_TOKEN"

# Скачать файл
curl -X GET "http://your-server:8000/logs/download/app_2026-01-02.log" \
  -H "Cookie: users_access_token=YOUR_TOKEN" \
  -o "app_2026-01-02.log"
```

### Python:
```python
import requests

# Получить список логов
response = requests.get(
    "http://your-server:8000/logs/list",
    cookies={"users_access_token": "YOUR_TOKEN"}
)
logs = response.json()

# Скачать файл
response = requests.get(
    "http://your-server:8000/logs/download/app_2026-01-02.log",
    cookies={"users_access_token": "YOUR_TOKEN"}
)
with open("app_2026-01-02.log", "wb") as f:
    f.write(response.content)
```

## ⚠️ Важные замечания

1. **Всегда используйте бинарный режим** для файлов `.log` в FileZilla
2. **API эндпоинт** - самый надежный способ скачивания
3. **Большие файлы** - используйте `/download-stream/` для файлов > 10MB
4. **Кодировка** - файлы логов всегда в UTF-8
5. **Расширение файла** - файлы `.log` автоматически скачиваются с расширением `.txt` для избежания блокировки антивирусом/браузером. Содержимое файла остается неизменным, просто измените расширение обратно на `.log` после скачивания, если нужно.

## 🔒 Решение проблемы с блокировкой антивирусом

Если браузер или антивирус блокирует скачивание файлов `.log`, это решено автоматически:

- **Файлы `.log` скачиваются с расширением `.txt`** - это предотвращает ложные срабатывания антивируса
- **Добавлены безопасные HTTP заголовки** - `X-Content-Type-Options`, `Content-Security-Policy` и другие
- **Правильное экранирование имени файла** - соответствует стандарту RFC 5987

После скачивания вы можете переименовать файл обратно в `.log`, если это необходимо. Содержимое файла остается полностью идентичным.

